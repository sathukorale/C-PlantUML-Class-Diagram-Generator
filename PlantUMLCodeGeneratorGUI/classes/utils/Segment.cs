namespace PlantUMLCodeGeneratorGUI.classes.utils
{
    class Segment
    {
        public readonly int Offset;
        public readonly int Length;

        public Segment(int offset, int length)
        {
            Offset = offset;
            Length = length;
        }
    }
}
